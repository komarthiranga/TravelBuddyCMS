import { HelpContent } from '@/site/components/HelpContent'

export const metadata = { title: 'Emergency help — TravelBuddy' }
export default function Page() { return <HelpContent emergency={true} /> }
